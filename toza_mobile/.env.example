# Points at the tozastarter REST API (api/index.php), the same backend
# the web SPA at toza.tozradar.com uses. Auth is per-user (see
# core/auth_kernel.dart) — there is no shared static secret anymore.
TOZA_API_BASE=https://toza.tozradar.com/api/index.php
TOZA_DEVICE_NAME=JONI_S26_ULTRA
TOZA_MODE=dev
